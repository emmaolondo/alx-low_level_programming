#include <stdio.h>
#include <stdlib.h>

/**
 *main - combination of two numbers
 *
 *Return: 0 (success)
 */
int main(void)
{
int m, n;
for (m = '0'; m <= '9'; m++)
for (n = m + '1'; n <= '9'; n++)
{
putchar(m);
putchar(n);
if (m == n){
putchar(',');
}
}
putchar('\n');
return (0);
}
