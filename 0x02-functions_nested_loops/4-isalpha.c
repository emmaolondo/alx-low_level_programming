#include "main.h"

/**
 *_isalpha - Checks for alphabetic characters
 *@c: Loval variable
 *
 *Return: 1 (success)
 */
int _isalpha(int c)
{
  if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
{
return (0);
}
else
{
return (0);
}
}
