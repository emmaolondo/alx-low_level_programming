#include "main.h"

/**
 *_abs - prints the absolute value of a number
 *@value: local variable
 *
 *Return: 0 (success)
 */
int _abs(int value)
{
if (value < 0)
{
return (-value);
}
else
return (value);
}
