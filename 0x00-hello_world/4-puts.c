#include <stdio.h>

/**
 * main -using the puts function to display the output of a string
 * Return: 0 (success)
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle");
return (0);
}
