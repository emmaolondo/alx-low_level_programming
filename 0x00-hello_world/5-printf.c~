#include <stdio.h>

/**
 *main - Using the printf function to display a string
 *
 *return: 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
