#include "main.h"

/**
 *mul - find the product of two integers
 *@a: Local variable
 *@b: local variable
 *
 *Return: 0 (success)
 */
int mul(int a, int b)
{
int product = a * b;
printf(product);
return (0);
}
