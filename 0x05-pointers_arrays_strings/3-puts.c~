#include "main.h"

/**
 *_puts - prints a string
 *@str: a string pointer
 *
 *Return: 0
 */
void _puts(char *str)
{
int i;/* count array elements*/
for (i = 0; str[i) != 0; i++)
{
_putchar(*(str + i));
}
_putchar('\n');
}
